<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="INotify.KToastView.View.KToastListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:INotify.KToastView.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:entity="using:INotifyLibrary.Model.Entity"
    Loaded="UserControl_Loaded"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="NormalTextStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="4"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ListView x:Name="ToastItems" ItemsSource="{x:Bind _VM.KToastNotifications, Mode=OneWay}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="entity:KToastNotification">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Style="{StaticResource NormalTextStyle}" Text="{x:Bind NotificationTitle, Mode=OneWay}"/>
                        <TextBlock Style="{StaticResource NormalTextStyle}" Text="{x:Bind NotificationMessage, Mode=OneWay}"/>
                        <TextBlock Style="{StaticResource NormalTextStyle}" Text="{x:Bind CreatedTime, Mode=OneWay}"/>
                        <TextBlock Style="{StaticResource NormalTextStyle}" Text="{x:Bind PackageId, Mode=OneWay}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
                  
                  
    </Grid>
</UserControl>
